from setuptools import setup


PACKAGE = "audit_utils"
VERSION = "0.01"

setup(
    name=PACKAGE,
    version=VERSION,
    description="The audit utilities we use ",
    author="Kelvin Fann",
    packages=["audit_utils"],
)